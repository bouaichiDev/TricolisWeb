import { ReferentialListPage } from './ReferentialListPage'

/** Types de regroupement — la façon dont les colis sont assemblés. */
export function GroupingTypeListPage() {
  return <ReferentialListPage kind="package-grouping-types" namespace="groupingTypes" />
}
